var $scope = $('#task-list-<%= @task_list.id %>-tasks');

$scope.find('.alert-empty-list').hide();
$scope.find('.tasks').prepend('<%=j render partial: 'task', locals: {task: @task} %>');
$scope.find('#task_name').val('');

